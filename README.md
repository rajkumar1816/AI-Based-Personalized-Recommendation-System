# AI-Based Personalized Recommendation System

Production-ready, modular Flask application that recommends personalized content using TF-IDF and cosine similarity.

Run:

```bash
pip install -r requirements.txt
python app.py
```

Open http://localhost:5000

Project structure and usage are inside the repository.